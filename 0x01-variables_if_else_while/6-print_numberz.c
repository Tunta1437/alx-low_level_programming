#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 * return: Always (success)
 *
 */
int main(void)
{
int d;
for (d = 0; d < 9; d++)
printf(d);
printf("\n");
return (0);
}
